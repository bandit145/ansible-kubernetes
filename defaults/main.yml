---
# defaults file for ansible-kubernetes
k8s_admin_config_location: /tmp/kube_admin.conf
k8s_kubevirt_install: true
k8s_argocd_install: true
k8s_linkerd_install: true

ovirt_release: 43
kubevirt_version: v0.26.5
calicoctl_version: 3.16.1
wireguard_config:
  apiVersion: crd.projectcalico.org/v1
  kind: FelixConfiguration
  metadata:
    name: default
  spec:
    wireguardEnabled: true